# UberBond Copilot instructions

Before substantive work, read `AI_START_HERE.md` and `AGENTS.md` in full, then follow `docs/UNIVERSAL_CONTEXT_PROTOCOL.md`.

Do not infer UberBond from the current file, issue, PR, UI, offer, or provider. Preserve the no-amputation law and terminal Sovereign Cognitive Continuum hierarchy.

When shell access exists, run `npm run brain` and `node scripts/context-universe.mjs --mission "<current request>"`, then inspect the emitted mandatory and mission-relevant paths before editing.

This file is only a compatibility pointer. `AGENTS.md`, canonical repo memory, current exact source, and durable external evidence remain authoritative.
