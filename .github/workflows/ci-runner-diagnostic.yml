name: CI Runner Diagnostic

on:
  pull_request:

jobs:
  runner-smoke:
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    steps:
      - name: Prove runner startup
        run: |
          echo "runner-started"
          uname -a
          node --version
