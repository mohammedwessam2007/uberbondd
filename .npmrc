# UberBond permits exactly the reviewed embedded PostgreSQL platform fixture to
# run its required lifecycle hydration. npm 11 otherwise blocks dependency
# install scripts by default, leaving initdb non-executable on Vercel and making
# Mutation War fail before any mutant can run.
allow-scripts=@embedded-postgres/linux-x64@18.4.0-beta.17
