# UberBond Universal AI Entry Point

If you are an AI, coding agent, research agent, IDE assistant, terminal agent, or fresh chat opening this repository, **do not infer UberBond from the newest file, PR, offer, UI, issue, or active mission**.

UberBond is a long-lived accumulated organism. A new context window is not a new project.

## Mandatory first action

1. Refresh the latest `main` and record the exact head SHA.
2. Read `AGENTS.md` in full.
3. Read `docs/UNIVERSAL_CONTEXT_PROTOCOL.md` in full.
4. Follow the startup sequence in `AGENTS.md` without narrowing the project before the terminal North Star, Total Brain, Master Memory, Perpetual Frontier/GENESIS, current handoff, and current state have been reconciled.
5. In a real repository host, run:

```bash
npm run brain
node scripts/context-universe.mjs --mission "<the user's actual request>"
```

6. Read every mandatory path emitted by the context-universe command, then read the highest-ranked mission-relevant files before acting.
7. Inspect current open/recent PRs, issues, branches, runtime/provider receipts, and external evidence when the mission depends on present-tense truth.

If `npm run brain` is unavailable, execute the documented read sequence manually. If Git or repository tools are unavailable, say that exact limitation instead of reconstructing UberBond from guesswork.

## No-amputation law

Never silently delete, forget, narrow away, or redefine an older UberBond goal, program, mechanism, donor, capability, offer family, life-system objective, research lineage, runtime architecture, or named initiative merely because a newer mechanism exists.

Supersession means: preserve the purpose, provenance, replacement, reason, truth class, and resurrection path where useful.

The terminal identity is the **Sovereign Cognitive Continuum**. The Personal Civilization Engine is the highest contained life-system organism. Economic autonomy is a supporting organ. Current commercial experiments are subordinate experiments. Infrastructure providers and payment rails are replaceable organs, not UberBond's identity.

## Context principle

**Resident context should be small; retrievable context should be complete.**

Do not try to stuff the whole repository, historical corpus, chats, logs, archives, and provider state into one prompt. Instead:

- keep the constitutional kernel resident;
- keep exact current truth resident;
- use repository-native indexes and hashes for total discoverability;
- retrieve mission-relevant source at execution time;
- preserve provenance and contradictions;
- never promote memory, simulation, documentation, or an old handoff into external proof.

## Truth precedence

Use the precedence defined by `AGENTS.md`, `NORTH_STAR.md`, `UBERBOND_BOOTSTRAP.json`, and the current repository/runtime evidence. In general:

`CURRENT EXACT SOURCE + DURABLE EXTERNAL RECEIPTS > CURRENT GENERATED STATE > CURRENT HANDOFF > CANONICAL MEMORY > HISTORICAL DONORS > CHAT RECOLLECTION > GUESS`

Historical breadth prevents forgetting. It does not override newer exact source or provider/customer/payment reality.

## Minimal recovery phrase

If the founder says only `continue`, `go`, `finish`, `resume`, or equivalent, do **not** ask for the project to be retold. Reconstruct context using this file, `AGENTS.md`, and `docs/CROSS_CHAT_CONTINUITY.md`, then continue from exact current truth.
