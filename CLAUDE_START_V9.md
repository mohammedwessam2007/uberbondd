# Claude Code V9 start

Run `python3 scripts/materialize-inevitability-v9.py` from repo root. Require `LOSSLESS_VERIFIED` and SHA-256 `8ad73f53116107acea824f6847aa6f29845e263b3ec01d241b90aae5719fe55d`. Then treat `.uberbond/v9/immutable/UBERBOND_NULLSTAR_OMEGA_INFINITY_INEVITABILITY_ENGINE_V9.txt` as immutable canonical V9, compile/index it progressively, and execute it from exact-current repo truth. Summaries/indexes are derived caches only and never replace the canonical reconstruction. Persist checkpoints across context windows. Do not stop at materialization.
