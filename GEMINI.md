# Gemini / Google AI bootstrap for UberBond

Before substantive work in this repository:

1. Read `AI_START_HERE.md`.
2. Read `AGENTS.md` in full.
3. Follow `docs/UNIVERSAL_CONTEXT_PROTOCOL.md`.
4. Refresh current `main`; do not trust remembered SHAs.
5. Run `npm run brain` and `node scripts/context-universe.mjs --mission "<current request>"` when shell access exists.
6. Preserve the no-amputation law and current truth hierarchy.

This file is a compatibility shim only. It does not override `AGENTS.md`, terminal North Star canon, or exact current source/runtime evidence.
