# GENESIS cognition runtime activation — 2026-09-22

The GENESIS Reality heartbeat now materializes cognition jobs from the highest-utility unimplemented candidates.

Live observed state at activation:
- Supabase `genesis-reality` v4: ACTIVE.
- Supabase `genesis-cognition-node` v1: ACTIVE.
- `genesis-reality-heartbeat`: every 10 minutes.
- 36 candidates tracked; 1 source-implemented; 35 probe-ready.
- 4 zero-spend cognition jobs queued on the first cognition-aware tick.
- 0 callable cognition suppliers.
- paid-provider mode disabled; daily cognition spend cap = 0.

The first queue contains Thermodynamic Compute Scheduler, Dusty Silicon Federation, Autonomous Failure-to-Patch Reflex and Blackout-First Compute Planner.

The runtime is deliberately fail-closed. The HP supplier remains non-callable because its last real agent receipt was legacy v1 and Ollama was unreachable. The OpenAI Platform account exposes an API project target, but that is not a runtime credential and does not authorize spend.

The next supplier may be local, cheap cloud or frontier. It must present identity, model/provider provenance, capabilities and callability evidence, and it can lease only jobs whose class and cost envelope admit it.

Truth boundary: persistent job creation is proven. Autonomous semantic inference is not yet proven because no model supplier is currently callable.
