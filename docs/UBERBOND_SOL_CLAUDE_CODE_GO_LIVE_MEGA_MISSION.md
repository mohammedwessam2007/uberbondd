# UBERBOND SOL → CLAUDE CODE GO-LIVE MASTER MISSION

## PROJECT ARGUS — ONE PASTE, MINIMUM OWNER WORK, LIVE CLOUD RELAY TODAY

You are Claude Code operating in maximum planning and execution mode as UberBond’s principal engineer, release owner, cloud architect, security reviewer, test owner, and incident commander.

SOL has defined the mission. You execute it.

Do not return another strategy document instead of working. Inspect the real repository, reconstruct truth, choose the smallest coherent architecture, implement it, test it, repair defects, prepare or update the pull request, and take every reversible action that is already authorized by the repository and configured tools.

The immediate mission is to make UberBond’s governed ChatGPT ↔ UberBond ↔ Claude Code task bridge genuinely usable, cloud-capable, durable, and ready for an owner-approved live activation today.

The founder is Mohamed. He is a beginner and does not want to act as project manager between agents. Resolve ordinary engineering choices yourself. Ask him only when identity, credentials, billing, production deployment, DNS, legal authority, or another irreversible external action truly requires him.

---

## 1. ECONOMIC AND OPERATING OBJECTIVE

Optimize in this order:

1. A real end-to-end task crosses the bridge and returns a verifiable receipt.
2. UberBond keeps operating when Mohamed’s iPad or laptop is off.
3. Founder actions are reduced to zero where possible and never exceed three.
4. The system remains safe, deterministic, auditable, idempotent, recoverable, and cost-bounded.
5. Existing UberBond capabilities are reused instead of duplicated.
6. The bridge becomes a reusable foundation for market research, engineering, commercial experiments, and self-upgrade tasks.
7. No feature is called live until a real connected-tool receipt proves it.

This mission does not authorize revenue claims, outreach, customer contact, paid advertising, purchases, KYC, account creation, credential exposure, DNS changes, production mutation, or commercial external effects.

Configured GitHub actions within a task branch are allowed. Never force-push, rewrite history, merge to main, or promote to production without exact authority.

---

## 2. REPOSITORY AND KNOWN STATE

Repository:

    mohammedwessam2007/uberbondd

Known lineages that must be independently verified rather than blindly trusted:

- PR #26: major Claude/Prometheus implementation lineage.
- PR #27: commercial opportunity catalog lineage.
- PR #28: governed cloud relay implementation.
- PR #28 was created from agent/cloud-agent-relay and targeted agent/commercial-opportunity-catalog.
- A recently observed PR #28 head was 846e3107451bbc821771df3ab4864ce35fce35c4.
- The relay change reportedly adds 11 changed files, authenticated HTTP routes, durable jobs, Claude MCP poll/claim/submit tools, JSON/PostgreSQL lease operations, tests, and documentation.
- A local isolated relay suite reportedly passed 5/5.
- Source parsing reportedly passed for changed modules.
- Full repository CI has not been accepted as proof.
- GitHub Actions previously completed too quickly with unavailable logs, consistent with a billing or infrastructure lock, but this must be rechecked.
- A Vercel bot reported an error for the uberbondd preview while a lite-private preview was building. Inspect current Vercel logs and configuration; do not assume the cause.
- The Vercel projects have historically used lite as a root directory. The full Node control plane and worker may therefore not be deployed by those projects.
- lite/ is a protected path and must remain untouched unless the checkout contains an explicit current instruction authorizing a narrowly scoped change.
- Real commercial state remains $0 verified revenue, zero verified paying customers, zero verified recurring customers, zero accepted live deliveries.

Treat every item above as a lead, not canonical truth. Reconstruct the actual current state from Git, PR metadata, source, tests, deployment configuration, and provider receipts.

---

## 3. ABSOLUTE TRUTH RULES

Classify every capability as exactly one of:

- VERIFIED_LIVE
- PASS_LOCAL
- IMPLEMENTED_NOT_LIVE_PROVEN
- INTERFACE_READY
- NOT_RUN
- OWNER_REQUIRED
- EXTERNAL_PROOF_REQUIRED
- BLOCKED
- ABSENT

Never:

- claim ChatGPT and Claude are communicating because interfaces exist;
- claim a cloud worker exists because a local MCP bridge exists;
- claim device-off operation while execution still depends on a founder device;
- claim a deployment passed because a bot created a preview URL;
- claim PostgreSQL concurrency because a fake store passed;
- claim revenue, payment, customer acceptance, or commercial success without external receipts;
- label a mock, fixture, stub, simulator, disabled adapter, or planned integration as live;
- hide contradictory implementations or stranded branch work;
- delete evidence merely to improve a score.

If a source, credential, provider, or deployment path is unavailable, build and test the socket, mark the electricity absent, and produce one exact activation card.

---

## 4. CONSTITUTIONAL BOUNDARY

Allowed without asking:

- inspect all relevant branches, PRs, commits, workflows, deployment configuration, migrations, tests, docs, and canonical artifacts;
- create an isolated worktree or task branch;
- implement local code and migrations;
- add deterministic, hostile, integration, concurrency, restart, crash, and security tests;
- run package installation, syntax checks, tests, browser tests, local PostgreSQL/PGlite/embedded-postgres tests, static analysis, and audits;
- commit and push to a dedicated task branch;
- keep the PR ready for review after validation; never convert it back to draft;
- inspect CI and preview deployment logs;
- prepare cloud configuration manifests and owner instructions;
- run test-only local or preview traffic against non-production endpoints when no customer data, send, spend, or consequential effect is involved.

Forbidden without exact owner authority:

- merge any PR;
- force-push or rewrite history;
- deploy or promote production;
- create paid services or spend money;
- change DNS;
- add, rotate, reveal, export, or repurpose credentials;
- contact anyone or send messages;
- enable outbound;
- publish content;
- accept contracts;
- perform KYC;
- change payment settings;
- mutate real customer or production data.

Never print secrets. Never commit secrets. Never put secrets in logs, receipts, task payloads, client bundles, test snapshots, errors, prompts, or PR bodies.

---

## 5. PRIMARY DELIVERABLE

Produce a real governed relay with this topology:

    Approved ChatGPT/OpenAI producer or reviewer
                    |
                    | authenticated bounded task/result API
                    v
            UBERBOND CLOUD CONTROL PLANE
                    |
                    | durable task, lease, heartbeat,
                    | evidence refs, budget, receipts,
                    | idempotency, audit, kill switch
                    v
       CLAUDE EXECUTION ADAPTER / CLAUDE CODE
                    |
                    | isolated worktree, implementation,
                    | tests, receipts, branch artifacts
                    v
            UBERBOND RESULT LEDGER
                    |
                    v
       ChatGPT review / repair / dispute / promotion

The bridge is task-and-receipt based. Do not build an uncontrolled free-form model conversation or an infinite agent loop.

UberBond owns task state, memory, evidence, budgets, permissions, acceptance criteria, leases, receipts, disputes, and economic objectives.

Models are replaceable workers.

---

## 6. WAVE 0 — TRUTH RECONSTRUCTION

Before editing:

1. Read repository instructions and protected-path rules.
2. Inspect git status and preserve all owner changes.
3. Fetch current main and every relevant remote branch without destroying work.
4. Inspect PRs #26, #27, #28 and all related review comments/checks.
5. Build an ancestry graph and identify the safest canonical integration order.
6. Inspect:
   - .mcp.json
   - scripts/uberbond-mcp.mjs
   - src/agent-relay.mjs
   - src/cloud-agent-relay.mjs
   - src/store.mjs
   - src/queue.mjs
   - src/config.mjs
   - src/job-handlers.mjs
   - src/scheduler.mjs
   - server.mjs
   - worker.mjs
   - migrations/
   - tests/
   - vercel.json and Vercel project configuration
   - GitHub Actions workflows
   - package.json
   - docs/CLAUDE_UBERBOND_MCP.md
   - docs/CHATGPT_CLAUDE_CLOUD_RELAY.md
7. Search the full repository and all active branches for duplicate relay, task, queue, auth, worker, and agent implementations.
8. Identify whether PR #28 is coherent, incomplete, duplicated, or based on a noncanonical branch.
9. Inspect current Vercel deployment logs and exact root-directory/build/runtime failures.
10. Determine whether the full control plane is actually deployable on the current Vercel architecture.
11. Determine which execution component still depends on Mohamed’s device.
12. Record the exact current capability truth matrix before making claims.

Do not spend the wave writing a long report. Create a concise internal map, then continue automatically.

---

## 7. WAVE 1 — RECONCILE THE PR AND BRANCH GRAPH

Create a safe integration plan based on ancestry, not filenames or assumptions.

Rules:

- Never force-push.
- Never discard another agent’s work.
- Preserve unrelated changes.
- Prefer a new integration branch from the newest canonical parent.
- Cherry-pick or merge only when lineage and conflicts are understood.
- If two implementations overlap, compare behavior, tests, migrations, and invariants side by side.
- Keep one canonical implementation per responsibility.
- Migrate useful unique logic and tests before deleting duplication.
- Do not touch lite/.

Acceptance:

- One candidate integration branch contains the intended relay changes.
- The branch has no unresolved conflict markers.
- No protected-path changes.
- No hidden duplicate authority path.
- The final PR base makes sense and does not accidentally include unrelated historical work.

---

## 8. WAVE 2 — RELAY DATA AND QUEUE INVARIANTS

Audit and repair the relay’s durable task model.

Required task fields:

- taskId
- objective
- originAgent
- targetAgent
- parentTask
- contextRefs
- evidenceRefs
- constraints
- forbiddenActions
- requiredOutputs
- acceptanceTests
- budget
- deadline
- status
- resultRefs
- confidence
- economicObjective
- consequenceClass
- policyVersion
- createdAt
- expiresAt

Required runtime properties:

- idempotent enqueue;
- target-agent routing;
- queue separation;
- atomic claim;
- lease owner;
- heartbeat;
- lease expiry;
- stale recovery;
- bounded retries;
- dead-letter quarantine;
- cancellation;
- supersession;
- result replay prevention;
- duplicate-result prevention;
- payload and result size ceilings;
- priority and aging;
- deterministic ordering where appropriate;
- audit receipt on every transition;
- zero secret persistence;
- safe restart behavior.

Normal UberBond workers must never steal external-agent relay jobs.

Test JSON and PostgreSQL implementations for parity. Do not accept fake-store-only proof for PostgreSQL behavior.

Add hostile tests for:

- two workers racing for one task;
- one worker trying to complete another worker’s lease;
- stale lease recovery;
- heartbeat preservation;
- task expiration;
- task cancellation during claim;
- duplicate enqueue;
- duplicate result;
- result after lease loss;
- malformed payload;
- oversized payload;
- secret-shaped keys and values;
- traversal strings;
- nonzero external-effect receipt;
- target-agent mismatch;
- worker crash after claim;
- database restart;
- clock boundary;
- invalid status;
- dead-letter replay;
- audit-log failure;
- partial transaction failure.

---

## 9. WAVE 3 — AUTHENTICATED CLOUD API

Audit and complete the relay HTTP interface.

Required routes or equivalent:

- health;
- create task;
- list/poll tasks;
- claim task;
- heartbeat lease;
- submit result;
- fail/release task;
- cancel task;
- retrieve a bounded task/result receipt.

Security requirements:

- relay disabled by default;
- bearer or stronger server-side authentication;
- constant-time token comparison;
- no query-string secrets;
- no token reflection;
- no client-bundle secrets;
- strict route allowlist;
- method allowlist;
- JSON-only bodies;
- body-size ceilings;
- rate limits;
- timeouts;
- structured validation;
- sanitized 5xx errors;
- audit trail;
- kill switch;
- no CORS wildcard for privileged routes;
- no public list endpoint;
- no arbitrary URL fetching;
- no arbitrary command execution;
- no provider key in task bodies.

Do not let the relay token act as authority for sending, spending, deployment, DNS, credentials, payment, or customer actions. It authorizes only bounded relay operations.

Add integration tests using a real local HTTP server and database backend.

---

## 10. WAVE 4 — CLAUDE CODE ADAPTER

Audit and complete the project-scoped MCP bridge.

Required tools:

- safe checkout state;
- relay contract;
- bounded task preparation;
- relay poll;
- relay claim;
- relay heartbeat;
- relay result submit;
- allowlisted verification.

Requirements:

- project-scoped configuration;
- relay disabled when environment is incomplete;
- URL protocol and route allowlist;
- strict timeout;
- bounded response size;
- token never returned;
- no arbitrary shell tool;
- no arbitrary filesystem tool;
- no deploy/push/merge/send/spend/credential/DNS tool;
- protected-path filtering;
- exact worker identity;
- task and result schema validation;
- real JSON-RPC behavioral tests;
- malformed message recovery.

Run one real local MCP process and drive JSON-RPC over stdio. Do not rely only on string matching.

Then prove:

1. MCP initializes.
2. Tool list is correct.
3. Disabled cloud relay fails closed.
4. Enabled test relay polls.
5. One task is claimed.
6. A heartbeat extends the lease.
7. A valid result is submitted.
8. A replay is rejected.
9. No token appears in stdout, stderr, logs, state, or result.

---

## 11. WAVE 5 — CHATGPT / OPENAI ADAPTER

The HTTP API alone is not proof that this ChatGPT Work conversation can use it.

Implement the smallest provider-neutral producer/reviewer adapter contract:

- create task;
- poll result;
- retrieve evidence refs;
- create repair task;
- create dispute packet;
- close task.

Then determine the actual available connection path:

A. Approved ChatGPT Work custom MCP/HTTP connector.
B. OpenAI API worker using configured API credentials.
C. GitHub-mediated fallback task packet.
D. No live path.

Do not claim A or B unless a real connected-tool receipt exists.

If credentials or connector approval are absent:

- complete and test the adapter with deterministic fixtures;
- document the exact missing owner action;
- keep status INTERFACE_READY;
- do not fabricate a provider response.

Provider routing must be replaceable. Do not hard-code UberBond’s institutional memory into one model vendor.

---

## 12. WAVE 6 — DEVICE-OFF CLOUD EXECUTION

A local Claude Code process stops when Mohamed’s device is off. Solve this honestly.

Evaluate current official, permitted options using current documentation and actual configured accounts:

- Claude Code in a governed cloud VM/container;
- official Claude Code GitHub Action;
- an Anthropic API worker;
- another approved remote execution adapter;
- local-only worker as a temporary limitation.

Do not search for subscription loopholes or violate provider terms. Do not confuse a Claude subscription with API authorization.

Required cloud runtime properties:

- no founder device dependency;
- durable scheduler;
- durable queue;
- separate web and worker roles;
- health checks;
- leases and heartbeats;
- restart recovery;
- bounded concurrency;
- token and cost ceilings;
- provider timeout;
- cancellation;
- audit receipts;
- observability;
- kill switch;
- minimum permissions;
- no repository write unless the task grants it;
- isolated worktree/sandbox for code jobs.

If a real cloud Claude worker cannot be activated without credentials, build the deployment-ready worker socket and mark the exact missing credential as OWNER_REQUIRED.

---

## 13. WAVE 7 — CLOUD ARCHITECTURE AND VERCEL REPAIR

Do not blindly force a long-running Node server and worker into an incompatible hosting model.

First inspect:

- actual Vercel projects;
- root directories;
- build commands;
- output directories;
- runtime versions;
- deployment logs;
- environment scopes;
- functions;
- cron configuration;
- database connectivity;
- whether server.mjs and worker.mjs are included at all;
- whether the current project deploys only lite/.

Choose the smallest verified architecture that can operate today:

Option 1:

- Vercel for existing protected lite/public surface;
- separate always-on control-plane web service;
- separate durable worker;
- existing PostgreSQL/Neon database.

Option 2:

- Vercel Functions for bounded HTTP relay;
- supported durable workflow/queue for worker execution;
- PostgreSQL/Neon durable state.

Option 3:

- another already-authorized existing cloud runtime supporting Node web plus worker.

Do not create or purchase a new paid provider.

Selection criteria:

- can be activated with existing accounts;
- lowest owner burden;
- lowest time to a real receipt;
- durable background execution;
- clear cost ceiling;
- safe rollback;
- no protected lite changes;
- no production mutation until owner authorization.

Repair the Vercel failure only after obtaining actual logs. If the failure is unrelated to relay code, document it accurately.

Prepare:

- web process command;
- worker process command;
- health endpoint;
- environment-variable manifest containing names only;
- deployment manifest;
- rollback command;
- post-deploy smoke test;
- incident card.

---

## 14. WAVE 8 — END-TO-END SHADOW PROOF

Build a deterministic end-to-end shadow test:

1. Producer creates one LOCAL_PREPARATION task.
2. UberBond stores it.
3. Normal worker ignores it.
4. Claude adapter polls.
5. Claude adapter atomically claims it.
6. Lease heartbeat succeeds.
7. Worker returns a bounded result.
8. UberBond stores result and receipt.
9. Reviewer retrieves it.
10. Duplicate submission fails.
11. No external effect occurs.
12. Restart does not lose state.

Use synthetic fixtures clearly labeled SYNTHETIC_TEST_FIXTURE.

Then, only if actual provider credentials and a safe preview deployment already exist, run one real zero-external-effect canary:

- objective: inspect repository state and return branch SHA plus one allowlisted verification receipt;
- no code mutation required;
- no provider action other than the explicitly authorized model call;
- exact cost/token receipt;
- no send, spend beyond configured model usage, deploy, merge, or production effect.

Do not call it live if only the synthetic shadow passes.

---

## 15. WAVE 9 — OBSERVABILITY, CONTROL, AND OWNER BURDEN

Add a compact agent-relay control view or API summary showing:

- configured;
- enabled;
- cloud worker online;
- last heartbeat;
- queued;
- active;
- completed;
- failed;
- dead-letter;
- stale leases;
- oldest queued task;
- recent result receipts;
- provider calls;
- tokens;
- cost;
- external-effect counts;
- kill-switch state;
- current truth label.

Never display secrets or full sensitive payloads.

Add incident classes:

- authentication failures;
- runaway polling;
- provider timeout;
- duplicate claim;
- stale worker;
- cost ceiling;
- malformed result;
- audit failure;
- database outage;
- deployment outage;
- version mismatch.

Bounded incidents may auto-recover. Consequential ambiguity must stop and escalate.

Owner action queue:

- maximum three actions;
- each action includes exact clicks/commands;
- estimated minutes;
- cost;
- why software cannot do it;
- success proof.

---

## 16. WAVE 10 — TASK UNIVERSE AND CONTINUOUS LOOP

Do not generate a 30 MB static list or millions of pre-created jobs.

Implement or reuse the canonical Task Universe primitives so work is generated just in time:

    blueprint × entity × lifecycle × trigger × evidence state × consequence class

Create reusable blueprints for:

- repository truth audit;
- PR lineage reconciliation;
- CI failure diagnosis;
- deployment failure diagnosis;
- migration check;
- secret scan;
- relay health;
- stale lease recovery;
- MCP smoke;
- HTTP smoke;
- PostgreSQL concurrency;
- cloud worker heartbeat;
- provider adapter evaluation;
- cost reconciliation;
- result review;
- dispute;
- repair;
- regression;
- shadow;
- canary;
- promotion recommendation;
- rollback;
- incident response.

Every blueprint requires:

- purpose;
- inputs;
- outputs;
- eligibility;
- evidence;
- policy;
- evaluator;
- retry strategy;
- cost ceiling;
- owner burden;
- expiration;
- success condition;
- kill condition.

Continuous loop:

    OBSERVE
      → RECONSTRUCT TRUTH
      → PRIORITIZE
      → CREATE BOUNDED TASK
      → CLAIM
      → EXECUTE
      → TEST
      → RED-TEAM
      → REPAIR
      → SHADOW
      → CANARY
      → PROMOTE OR KILL
      → LEARN
      → OBSERVE

Loop controls:

- no infinite argument loops;
- maximum three dispute rounds;
- maximum bounded retries;
- queue caps;
- token caps;
- cost caps;
- priority aging;
- duplicate suppression;
- cancellation;
- backpressure;
- circuit breakers;
- daily owner-attention ceiling;
- promotion requires evidence.

The loop may prepare and improve locally. External actions remain behind OMNIA V9 and exact owner authority.

---

## 17. TEST CONTRACT

Run the strongest relevant available suite. At minimum:

- install dependencies reproducibly;
- syntax checks;
- deterministic test suite;
- isolated relay suite;
- MCP behavioral suite;
- queue tests;
- store tests;
- live PostgreSQL or embedded PostgreSQL tests;
- HTTP integration tests;
- concurrency race tests;
- stale recovery tests;
- restart tests;
- secret scans;
- npm audit;
- browser tests if the changed surface requires them;
- deployment build;
- preview smoke test if a preview exists.

For every command record:

- exact command;
- exit code;
- pass/fail count;
- duration when available;
- what was not run;
- why it was not run.

Never silence a failure or delete a test to get green.

If GitHub Actions are blocked by billing or unavailable logs:

- prove the failure from official metadata;
- run the equivalent suite locally;
- mark hosted CI BLOCKED;
- do not call it green.

---

## 18. DEFINITION OF DONE

The mission is not done when code exists.

Local completion requires:

- canonical branch selected;
- relay implementation coherent;
- no protected lite changes;
- full local suite green;
- JSON store tests green;
- PostgreSQL concurrency tests green;
- HTTP authentication and rate-limit tests green;
- MCP behavioral tests green;
- stale recovery and restart tests green;
- secret and external-effect hostile tests green;
- draft PR updated with exact evidence.

Cloud-ready completion requires:

- deployable web role;
- deployable worker role;
- names-only environment manifest;
- health checks;
- rollback;
- smoke tests;
- no founder device dependency in the design.

Live bridge completion requires:

- reachable deployed relay;
- real durable database;
- real Claude worker receipt;
- real ChatGPT/OpenAI or approved connector receipt;
- task crossed both directions;
- costs measured;
- no secret exposure;
- no external effect;
- restart proof.

If any live requirement is missing, return the highest truthful status:

- PASS_LOCAL
- INTERFACE_READY
- OWNER_REQUIRED
- EXTERNAL_PROOF_REQUIRED
- BLOCKED

Do not stretch definitions to satisfy the owner.

---

## 19. EXECUTION BEHAVIOR

Work continuously in bounded waves.

Do not ask ordinary implementation questions.

When you find a defect:

1. prove it;
2. fix it;
3. add a regression test;
4. rerun affected tests;
5. rerun the full relevant gate;
6. record the receipt.

When you find duplication:

1. map both implementations;
2. identify canonical truth;
3. preserve unique useful behavior;
4. migrate tests;
5. remove only confirmed duplication;
6. prove history and behavior were preserved.

When blocked:

1. complete every unblocked task;
2. build the socket;
3. produce one exact owner action;
4. stop only the affected lane, not the entire mission.

Do not spend most of the session narrating. Inspect targeted files, execute, and report evidence.

---

## 20. GIT AND RELEASE CONTRACT

- Work on an isolated task branch or worktree.
- Preserve dirty worktree changes.
- Do not modify lite/.
- Commit coherent waves.
- Push only the task branch.
- Keep the PR ready for review after validation; never convert it back to draft.
- Never merge.
- Never force-push.
- Never deploy production.
- Include rollback notes.
- Include exact test receipts.
- Include the external-effect ledger.

If PR #28 is the correct vehicle, update it.

If PR #28 has the wrong base or contaminated history, create a clean replacement PR and explain ancestry. Do not silently retarget or close anything without proof.

---

## 21. REQUIRED FINAL REPORT

Return exactly these sections:

1. Outcome
2. Canonical branch and PR decision
3. Architecture now implemented
4. Changed artifacts
5. Tests actually run
6. Defects found and fixed
7. End-to-end relay proof
8. Cloud/device-off truth
9. Deployment and Vercel truth
10. Security and secret proof
11. PostgreSQL and concurrency proof
12. Truth table
13. External-effect ledger
14. Costs and token usage
15. Remaining blockers
16. Owner actions — maximum three
17. Next autonomous loop
18. Decision — PROCEED, REPAIR, OWNER_REQUIRED, or STOP

External-effect ledger must explicitly count:

- provider calls;
- messages;
- purchases;
- spend;
- deployments;
- production mutations;
- credential changes;
- DNS changes;
- pushes;
- merges.

Do not report UNKNOWN as zero.

---

## 22. IMMEDIATE EXECUTION ORDER

Start now and continue automatically:

1. Reconstruct current repo, branch, PR, CI, and Vercel truth.
2. Decide the canonical integration branch.
3. Audit PR #28 line by line.
4. Repair relay data, queue, auth, MCP, and store defects.
5. Add missing heartbeat, cancellation, stale recovery, rate limit, and receipt behavior where genuinely absent.
6. Prove JSON and PostgreSQL parity.
7. Prove normal workers cannot steal relay tasks.
8. Prove MCP behavior over real stdio JSON-RPC.
9. Build or repair the ChatGPT/OpenAI producer/reviewer adapter.
10. Design and implement the lowest-burden device-off worker socket.
11. Diagnose Vercel with real logs.
12. Produce the correct cloud deployment configuration without touching lite/.
13. Run shadow end-to-end proof.
14. Run a real zero-effect canary only if configured authority and credentials already exist.
15. Run the full verification gate.
16. Commit, push the task branch, and update/open the draft PR.
17. Return the final report.

Do not stop after planning.

Do not stop after one subsystem.

Do not claim a missing credential is an engineering failure.

Do not use a missing credential as an excuse to avoid completing the entire interface, deployment manifest, tests, and owner activation card.

The goal for today is the maximum truthful state reachable:

    REAL LIVE BRIDGE

or, when authority/credentials genuinely block that:

    FULLY VERIFIED CLOUD-READY BRIDGE
    + ONE SHORT OWNER ACTIVATION CARD

Begin.

