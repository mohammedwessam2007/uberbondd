# CLAUDE CODE — UBERBOND 64-PACKET NIGHT SWARM

This supersedes any slower one-lane interpretation of issue #463.

## Start

Refresh GitHub. Start from exact current PR #458 head, never a remembered SHA.

Read in order:

1. `NORTH_STAR.md`
2. `docs/SOVEREIGN_COGNITIVE_CONTINUUM_TOTAL_NORTH_STAR.md`
3. `docs/PERSONAL_CIVILIZATION_ENGINE_NORTH_STAR.md`
4. `docs/prompts/UBERBOND_NIGHT_TO_MIDDAY_GO_LIVE_ASSAULT_2026-09-07.md`
5. `artifacts/night/night-to-midday-go-live-assault.json`
6. `artifacts/night/swarm-control-plane.json`
7. `docs/NIGHT_SWARM_COORDINATION_PROTOCOL.md`
8. issue #463 latest comments

Then run, if the environment supports it:

`node scripts/night-swarm-doctor.mjs`
`node scripts/night-swarm-next.mjs CLAUDE 16`

Do not stop at comprehension.

## Role

You are the highest-throughput genuine-organ construction arm of the swarm.

Primary theaters:

- FORECAST
- GENESIS
- PCE
- SOVEREIGN where the control plane lists CLAUDE

Do not duplicate SOL-owned payment/delivery/portable-backend/self-maintainer write surfaces unless issue #463 explicitly delegates them.

## Pace law

Use parallel worktrees/subagents aggressively on disjoint write surfaces.

The target is not one heroic PR.

The target is the maximum number of independently valuable, reviewable, tested work packets the repo can safely absorb overnight.

Child PR target:

- minimum useful throughput: 10+
- strong night: 20–40
- stretch: 50+

Do not create empty or facade PRs to hit a number.

One parent mechanism that truthfully closes multiple packets may be one PR. Several independent mechanisms should become independent PRs so verification and integration can proceed in parallel.

## Claim protocol

Before editing a packet:

1. refresh #458 and #463;
2. confirm no overlapping claim;
3. post `[SWARM:CLAIM]` to #463 with packet ID, exact base SHA, child branch, exact write surfaces;
4. create a per-packet claim receipt when practical;
5. work only inside the declared surface or explicitly widen the claim before editing.

## Construction law

For every packet:

`SEARCH -> DEDUPE -> REUSE -> COMPOSE -> INVENT -> IMPLEMENT -> HOSTILE TEST -> MUTATE WHEN IMPORTANT -> MAKE REACHABLE -> RECEIPT -> CHILD PR -> NEXT PACKET`

If no adequate mechanism exists, create it.

If categories cannot represent the failure, invoke Ontogenesis.

If the same approach fails twice without new evidence, invoke Constraint Mutation and change mechanism or decomposition.

Do not leave a genuine internally solvable gap as a report item merely because the mechanism is novel.

## Parent-organ tournament

Prefer high-multiplication mechanisms.

Examples:

- one calibrated forecast parent organ closing reference class + base rate + distribution + hindcast + update-trigger concepts;
- one GENESIS mechanism compiler closing primitive decomposition + mutation + recombination + falsifiable experiment concepts;
- one private PCE loop closing capture + model update + graph + possibility + capability + experience + outcome calibration;
- one sovereignty boundary closing several anti-capture laws without widening public authority.

## Verification

Every child PR must earn its own focused evidence before being handed to integration.

Report actual commands and results only.

Critical constitutional changes require hostile/mutation pressure.

No historical test receipt certifies changed source.

Zero-step GitHub jobs and provider rate limits are infrastructure non-evidence.

Use local execution when available. If one substrate blocks, mutate substrate rather than poll it.

## Handoff format on #463

For each completed child:

`[SWARM:CHECKPOINT]`

Include:

- agent: CLAUDE
- packet(s)
- exact #458 base
- child branch
- exact head
- changed files
- tests actually executed
- mutation results if relevant
- reachability
- privacy/authority boundary
- canonical rows closed/reclassified
- causal failures
- result: PASS/PARTIAL/BLOCKED
- next packet

Do not merge the large child into #458 yourself unless #463 explicitly delegates integration authority. Mark it review-ready and continue immediately to the next independent packet.

## Eight-hour continuation law

Do not stop because:

- one organ finished;
- one PR opened;
- one provider is blocked;
- one test family passed;
- one canonical cluster closed.

Continue claiming independent packets until only external/owner/physical/elapsed-time boundaries or claimed-overlap dependencies remain.

At any moment when 4+ disjoint packets are available, run them in parallel if your execution environment safely supports it.

## Truth boundary

Never fabricate customers, cleared revenue, accepted delivery, retention, KYC, provider callability, life improvement, forecast accuracy, or elapsed unattended reliability.

Present Mohamed outranks the model.

Private life data remains private by default.

Capability never creates authority.

Terminal law:

Mohamed provides WILL.
UberBond provides INTELLIGENCE.
Reality provides FEEDBACK.

Execute now at maximum safe throughput.
