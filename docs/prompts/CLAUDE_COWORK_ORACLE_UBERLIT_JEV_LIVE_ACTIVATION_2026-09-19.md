# Claude Cowork Mission — Finish UberBond Jev Live Activation

Open the **UberBond repository folder** as the Cowork working folder.

Then execute this mission end-to-end:

> Invoke the project skill `oracle-free-uberlit-jev-activation`. Recover exact current UberBond truth first, then finish the zero-new-spend Oracle Always Free ARM64 → UberCel → UberLit → Jev activation. Do routine work yourself across the repository, terminal and browser. Do not return a plan. Continue until the live Jev shadow receipt is proven or a genuine owner/auth/legal/capacity blocker is physically reached. At owner gates, ask Mohamed for exactly one concrete UI/auth action, then continue immediately after it is satisfied. Never ask him to paste the TypeSafe key into Claude; use the repository's hidden terminal secret installer. Never create paid Oracle resources or weaken UberBond safety/verification gates. Finish with exact receipts, current commit, UberOcean host-admission evidence, live service health, actual Jev provider/model/latency/cost, and a truthful READY / BLOCKED verdict.

Expected owner-only gates:

1. Oracle login / 2FA / terms / identity or payment-card verification if Oracle itself requires it.
2. Tailscale device authorization.
3. TypeSafe login / terms and hidden API-key paste into the terminal prompt.
4. A maximum `$0.001 USD` live-canary authorization only if the TypeSafe account would actually charge rather than consume free/prepaid credit.

Everything else is Cowork's job.

The live canary command is intentionally impossible to run without the explicit spend ceiling token:

`sudo bash /opt/uberlit/source/ops/sovereign/complete-live-jev-activation.sh --authorize-max-usd 0.001`

Do not run that command until the spend gate in the project skill is satisfied.
