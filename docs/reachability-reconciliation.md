# Reachability lineage reconciliation

This branch reconciles the live import graph with preserved classifications from prior UberBond lineages. It does not activate blocked capability paths or manufacture external authority.
