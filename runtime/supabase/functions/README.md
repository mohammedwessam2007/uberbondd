# Supabase GENESIS runtime

These files mirror the exact deployed source of the private Supabase GENESIS runtime as of 2026-09-22.

- `genesis-reality/index.ts`: live v4 reconciliation heartbeat.
- `genesis-cognition-node/index.ts`: live v1 authenticated supplier lease/receipt endpoint.

Deployment state is evidence recorded separately. Repository source does not itself prove that an Edge Function is deployed or healthy.
