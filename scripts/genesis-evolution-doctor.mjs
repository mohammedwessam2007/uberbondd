#!/usr/bin/env node
import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';import {buildGenesisEvidenceLedger,GENESIS_IMPLEMENTATION_EVIDENCE} from '../src/genesis-implementation-evidence-v2.mjs';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const required=['docs/PERPETUAL_FRONTIER_GENESIS_CANON.md','src/genesis-evolution-engine.mjs','src/genesis-implementation-evidence-v2.mjs','tests/genesis-evidence-ledger-v2.test.mjs','scripts/genesis-evolution-tick.mjs','data/genesis/impossible-tasks.json','data/genesis/architecture-assumptions.json'];
const missing=required.filter(r=>!fs.existsSync(path.join(root,r)));if(missing.length){console.log(JSON.stringify({ok:false,status:'GENESIS_EVOLUTION_INVALID',missing},null,2));process.exit(1);}
const markdown=fs.readFileSync(path.join(root,'docs/PERPETUAL_FRONTIER_GENESIS_CANON.md'),'utf8'),tasks=JSON.parse(fs.readFileSync(path.join(root,'data/genesis/impossible-tasks.json'),'utf8')),assumptions=JSON.parse(fs.readFileSync(path.join(root,'data/genesis/architecture-assumptions.json'),'utf8'));
const declared=[...new Set(Object.values(GENESIS_IMPLEMENTATION_EVIDENCE).flatMap(e=>[...e.sources,...e.tests]))],availablePaths=declared.filter(r=>fs.existsSync(path.join(root,r))),observedRuntimeReceipts=[...new Set(Object.values(GENESIS_IMPLEMENTATION_EVIDENCE).flatMap(e=>e.runtimeReceipts))].filter(r=>fs.existsSync(path.join(root,r)));
const ledger=buildGenesisEvidenceLedger({canonicalMarkdown:markdown,availablePaths,observedRuntimeReceipts});
const validTasks=tasks?.schemaVersion==='uberbond.genesis-impossible-tasks.v1'&&Array.isArray(tasks.tasks)&&tasks.tasks.length>0,validAssumptions=assumptions?.schemaVersion==='uberbond.genesis-architecture-assumptions.v1'&&Array.isArray(assumptions.assumptions)&&assumptions.assumptions.length>0,missingDeclaredEvidence=ledger.ok?ledger.entries.flatMap(e=>e.missingPaths||[]):[];
const ok=ledger.ok&&validTasks&&validAssumptions&&missingDeclaredEvidence.length===0&&ledger.implementedOrPartialCount===275&&ledger.canonOnlyCount===0;
const result={ok,status:ok?'GENESIS_275_SOURCE_TEST_CONTRACT_HEALTHY':'GENESIS_EVOLUTION_INVALID',ideaCount:ledger.ideaCount,implementationCounts:ledger.counts,maturityCounts:ledger.maturityCounts,implementedOrPartialCount:ledger.implementedOrPartialCount,canonOnlyCount:ledger.canonOnlyCount,impossibleTaskCount:tasks?.tasks?.length||0,antiUberBondAssumptionCount:assumptions?.assumptions?.length||0,observedRuntimeReceiptCount:observedRuntimeReceipts.length,missingDeclaredEvidence:[...new Set(missingDeclaredEvidence)],businessEffectAuthority:'NONE',truthBoundary:'DOCTOR_PROVES_275_INTERNAL_SOURCE_AND_TEST_FILE_CONTRACTS_ONLY; IT_DOES_NOT_PROVE_TEST_PASS_RUNTIME_OPERATION_EXTERNAL_VALUE_OR_1_IN_500_MILLION_RARITY'};
console.log(JSON.stringify(result,null,2));if(!ok)process.exitCode=1;
