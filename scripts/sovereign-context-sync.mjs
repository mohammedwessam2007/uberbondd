#!/usr/bin/env node
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { ZERO_EXTERNAL_EFFECTS } from '../src/effect-ledgers.mjs';
import { readContextJournalRuntime } from '../src/context-journal-runtime.mjs';
import { checkpointSovereignContext } from './sovereign-context-checkpoint.mjs';
import { mountSovereignContext } from './sovereign-context-mount.mjs';
import { migrateContextJournalToLayeredStore } from './sovereign-context-journal-migrate.mjs';
import { compactContextJournalLayeredStore } from './sovereign-context-journal-compact.mjs';

export const SOVEREIGN_CONTEXT_SYNC_VERSION='sovereign-context-sync-2.0.1';
function zeroEffects(){return structuredClone(ZERO_EXTERNAL_EFFECTS);}
function fail(reasonCodes,status='CONTEXT_SYNC_REFUSED',extra={}){return{ok:false,syncVersion:SOVEREIGN_CONTEXT_SYNC_VERSION,status,reasonCodes:[...new Set((reasonCodes||[]).filter(Boolean))],businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zeroEffects(),...extra};}
function lastCheckpoint(entries=[]){for(let index=entries.length-1;index>=0;index-=1){const entry=entries[index];if(entry?.event?.kind==='CONTEXT_CHECKPOINT'&&entry?.event?.subjectType==='BRAINSTATE')return entry;}return null;}
function boundedThreshold(value,fallback){const number=Number(value??fallback);return Number.isSafeInteger(number)&&number>=1&&number<=50000?number:null;}
function maintenance({journalPath,layeredStorePath,journal,migrateAt,compactAt,generatedAt}){
  if(journal.storageMode==='LAYERED_ARCHIVE_TAIL'&&journal.downgradeProtected!==true){const result=migrateContextJournalToLayeredStore({journalPath,layeredStorePath,activatedAt:generatedAt});if(result.ok){const rebound=readContextJournalRuntime({journalPath,layeredStorePath});return{ok:rebound.ok,status:'CONTEXT_MAINTENANCE_LAYERED_HARDENED',result,rebound};}return{ok:false,status:'CONTEXT_MAINTENANCE_HARDENING_FAILED',result,rebound:journal};}
  if(journal.storageMode==='LEGACY_JSONL'&&journal.entryCount>=migrateAt){const result=migrateContextJournalToLayeredStore({journalPath,layeredStorePath,activatedAt:generatedAt});if(result.ok){const rebound=readContextJournalRuntime({journalPath,layeredStorePath});return{ok:rebound.ok,status:result.status,result,rebound};}if(result.status==='CONTEXT_JOURNAL_MIGRATION_BUSY')return{ok:true,status:'CONTEXT_MAINTENANCE_DEFERRED_BUSY',result,rebound:journal};return{ok:false,status:'CONTEXT_MAINTENANCE_MIGRATION_FAILED',result,rebound:journal};}
  if(journal.storageMode==='LAYERED_ARCHIVE_TAIL'&&journal.tailEntryCount>=compactAt){const result=compactContextJournalLayeredStore({journalPath,layeredStorePath,activatedAt:generatedAt});if(result.ok){const rebound=readContextJournalRuntime({journalPath,layeredStorePath});return{ok:rebound.ok,status:result.status,result,rebound};}if(result.status==='CONTEXT_JOURNAL_COMPACTION_BUSY')return{ok:true,status:'CONTEXT_MAINTENANCE_DEFERRED_BUSY',result,rebound:journal};return{ok:false,status:'CONTEXT_MAINTENANCE_COMPACTION_FAILED',result,rebound:journal};}
  return{ok:true,status:'CONTEXT_MAINTENANCE_NOT_NEEDED',result:null,rebound:journal};
}

export function syncSovereignContext({rootDir=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),journalPath,layeredStorePath=null,capsuleCachePath=null,mountCachePath=null,mission=null,maxHistoricalEvents=24,generatedAt=new Date(),autoMigrateEntries=Number(process.env.UBERBOND_CONTEXT_AUTO_MIGRATE_ENTRIES||512),autoCompactTailEntries=Number(process.env.UBERBOND_CONTEXT_AUTO_COMPACT_TAIL_ENTRIES||2048)}={}){
  if(!journalPath)return fail(['journal-path-required']);const migrateAt=boundedThreshold(autoMigrateEntries,512);const compactAt=boundedThreshold(autoCompactTailEntries,2048);if(!migrateAt||!compactAt)return fail(['bounded-context-maintenance-thresholds-required']);
  const mounted=mountSovereignContext({rootDir,journalPath,layeredStorePath,capsuleCachePath,mountCachePath,mission,maxHistoricalEvents,generatedAt});if(!mounted.ok)return fail(['context-mount-required',...(mounted.reasonCodes||[])],'CONTEXT_SYNC_MOUNT_REFUSED',{contextMountStatus:mounted.status});
  let journal=readContextJournalRuntime({journalPath,layeredStorePath});if(!journal.ok)return fail(['verified-cognitive-journal-required',...(journal.reasonCodes||[])],'CONTEXT_SYNC_JOURNAL_REFUSED');const checkpoint=lastCheckpoint(journal.entries);const currentBrainstateId=mounted.mount?.brainstateId;if(!currentBrainstateId)return fail(['current-brainstate-id-required']);
  let checkpointResult=null;if(checkpoint?.event?.subjectId!==currentBrainstateId){checkpointResult=checkpointSovereignContext({rootDir,journalPath,layeredStorePath,capsulePath:capsuleCachePath,mission:mission||mounted.mount?.mission,generatedAt});if(!checkpointResult.ok)return fail(['context-checkpoint-required',...(checkpointResult.reasonCodes||[])],'CONTEXT_SYNC_CHECKPOINT_REFUSED',{checkpointStatus:checkpointResult.status});if(checkpointResult.brainstateId!==currentBrainstateId)return fail(['mounted-and-checkpointed-brainstate-mismatch'],'CONTEXT_SYNC_RACE_REFUSED',{mountedBrainstateId:currentBrainstateId,checkpointedBrainstateId:checkpointResult.brainstateId});journal=readContextJournalRuntime({journalPath,layeredStorePath});if(!journal.ok)return fail(['post-checkpoint-journal-rebind-required',...(journal.reasonCodes||[])],'CONTEXT_SYNC_JOURNAL_REFUSED');}
  const maintained=maintenance({journalPath,layeredStorePath,journal,migrateAt,compactAt,generatedAt});if(!maintained.ok)return fail(['context-storage-maintenance-failed',...(maintained.result?.reasonCodes||[])],'CONTEXT_SYNC_MAINTENANCE_REFUSED',{maintenanceStatus:maintained.status,checkpointAppended:checkpointResult!==null});journal=maintained.rebound;
  const remounted=checkpointResult?mountSovereignContext({rootDir,journalPath,layeredStorePath,capsuleCachePath,mountCachePath,mission,maxHistoricalEvents,generatedAt}):mounted;if(!remounted.ok)return fail(['post-maintenance-context-remount-required',...(remounted.reasonCodes||[])],'CONTEXT_SYNC_REMOUNT_REFUSED');
  return{ok:true,syncVersion:SOVEREIGN_CONTEXT_SYNC_VERSION,status:checkpointResult?'CONTEXT_SYNC_CHECKPOINT_APPENDED':'CONTEXT_SYNC_CURRENT_NO_CHECKPOINT',sourceCommit:remounted.mount.sourceCommit,brainstateId:remounted.mount.brainstateId,contextMountId:remounted.mount.contextMountId,journalEntryCount:journal.entryCount,journalTipDigest:journal.tipDigest,journalStorageMode:journal.storageMode||'LEGACY_JSONL',journalGenerationId:journal.generationId||null,journalTailEntryCount:journal.tailEntryCount||0,lastCheckpointEventId:checkpointResult?checkpointResult.eventId:checkpoint?.eventId||null,checkpointEventId:checkpointResult?.eventId||null,checkpointSequence:checkpointResult?.journalSequence||null,checkpointEntryDigest:checkpointResult?.journalEntryDigest||null,checkpointAppended:checkpointResult!==null,maintenanceStatus:maintained.status,maintenanceChanged:maintained.result?.ok===true&&maintained.status!=='CONTEXT_MAINTENANCE_NOT_NEEDED',maintenanceDeferred:maintained.status==='CONTEXT_MAINTENANCE_DEFERRED_BUSY',recompiledFromStale:mounted.mount.recompiledFromStale===true,businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zeroEffects()};
}
function argValue(name){const index=process.argv.indexOf(name);return index>=0&&index+1<process.argv.length?process.argv[index+1]:null;}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url)){try{const controlDir=path.resolve(process.env.UBERBOND_CONTROL_DIR||'/var/lib/uberbond-control');const journalPath=argValue('--journal')||process.env.UBERBOND_CONTEXT_JOURNAL_PATH||path.join(controlDir,'context','events.jsonl');const layeredStorePath=argValue('--layered-store')||process.env.UBERBOND_CONTEXT_LAYERED_STORE_PATH||null;const capsuleCachePath=argValue('--capsule-cache')||process.env.UBERBOND_BRAINSTATE_PATH||path.join(controlDir,'context','brainstate.json');const mountCachePath=argValue('--mount-cache')||process.env.UBERBOND_CONTEXT_MOUNT_PATH||path.join(controlDir,'context','mount.json');const mission=argValue('--mission');const result=syncSovereignContext({journalPath,layeredStorePath,capsuleCachePath,mountCachePath,mission});process.stdout.write(`${JSON.stringify(result,null,2)}\n`);if(!result.ok)process.exitCode=2;}catch(error){process.stderr.write(`${JSON.stringify({ok:false,status:'CONTEXT_SYNC_FAILED',reason:error?.message||'unknown-error',businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zeroEffects()},null,2)}\n`);process.exitCode=1;}}
