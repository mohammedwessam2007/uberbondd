import crypto from 'node:crypto';
import { MILLION_BRANCH_DOMAIN_NAMES, MILLION_BRANCH_OPERATOR_NAMES } from './million-branch-ideation-compat-data.mjs';
export const MILLION_BRANCH_IDEATION_GENOME_VERSION='uberbond.million-branch-ideation-genome-compat.v2';
export const MILLION_BRANCH_SOURCE_SHA256='4dc7269a2a8f6dfc1855d1f2cf7231d437b22fe948e4139808a1a97c937065e5';
export const MILLION_BRANCH_DOMAIN_COUNT=20,MILLION_BRANCH_OPERATOR_COUNT=50,MILLION_BRANCH_GENERATOR_COUNT=1000;
const zero=()=>({providerCalls:0,messages:0,spendCents:0,deployments:0,payments:0});
const slug=v=>String(v||'').toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'');
const norm=v=>String(v||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();
const bounded=(v,a,b)=>Number.isSafeInteger(Number(v))&&Number(v)>=a&&Number(v)<=b?Number(v):null;
export const MILLION_BRANCH_DOMAINS=Object.freeze(MILLION_BRANCH_DOMAIN_NAMES.map(name=>Object.freeze({id:slug(name),name,scope:`Search-space domain for ${name.toLowerCase()}. Personalization remains behind a separately authorized private context boundary.`})));
export const MILLION_BRANCH_OPERATORS=Object.freeze(MILLION_BRANCH_OPERATOR_NAMES.map(name=>Object.freeze({id:slug(name),name,template:`${name}: generate evidence-bound alternatives for {domain}; treat every output as a hypothesis until separately verified.`})));
export function buildMillionBranchIdeationGenome(){const out=[];let id=1;for(const d of MILLION_BRANCH_DOMAINS)for(const o of MILLION_BRANCH_OPERATORS)out.push(Object.freeze({id:id++,key:`${d.id}::${o.id}`,domainId:d.id,domainName:d.name,operatorId:o.id,operatorName:o.name,prompt:o.template.replaceAll('{domain}',d.name.toLowerCase())}));return Object.freeze(out);}
export function validateMillionBranchIdeationGenome(){const g=buildMillionBranchIdeationGenome(),reasons=[];if(g.length!==1000)reasons.push('generator-count-mismatch');if(new Set(g.map(x=>x.key)).size!==1000)reasons.push('duplicate-generator-key');if(new Set(g.map(x=>x.prompt.toLowerCase())).size!==1000)reasons.push('duplicate-generator-prompt');return{ok:!reasons.length,status:reasons.length?'MILLION_BRANCH_IDEATION_GENOME_INVALID':'MILLION_BRANCH_IDEATION_GENOME_HEALTHY',version:MILLION_BRANCH_IDEATION_GENOME_VERSION,sourceSha256:MILLION_BRANCH_SOURCE_SHA256,domainCount:20,operatorCount:50,generatorCount:g.length,reasonCodes:reasons,businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zero()};}
function domainMatches(values){if(!Array.isArray(values)||values.length>256)return null;const qs=values.map(norm).filter(Boolean);if(!qs.length)return[];return MILLION_BRANCH_DOMAINS.filter(d=>{const h=norm(`${d.id} ${d.name} ${d.scope}`);return qs.some(q=>h.includes(q)||q.includes(norm(d.name)));}).map(d=>d.id);}
export function buildIdeationActivationPlan({context,affectedDomains=[],maxGenerators=12,candidateBudgetPerGenerator=50,seed='uberbond'}={}){const c=String(context||'').trim(),s=String(seed||'').trim(),cap=bounded(maxGenerators,1,100),budget=bounded(candidateBudgetPerGenerator,1,100),matched=domainMatches(affectedDomains),reasons=[];if(!c)reasons.push('context-required');if(!s)reasons.push('seed-required');if(!cap)reasons.push('bounded-max-generators-required');if(!budget)reasons.push('bounded-candidate-budget-required');if(matched===null)reasons.push('bounded-affected-domains-required');if(reasons.length)return{ok:false,status:'MILLION_BRANCH_ACTIVATION_INVALID',reasonCodes:reasons,businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zero()};const all=buildMillionBranchIdeationGenome(),eligible=matched.length?all.filter(x=>matched.includes(x.domainId)):all,selected=eligible.map(item=>({item,score:crypto.createHash('sha256').update(`${s}|${c}|${item.key}`).digest('hex')})).sort((a,b)=>a.score.localeCompare(b.score)||a.item.id-b.item.id).slice(0,Math.min(cap,eligible.length)).map(x=>x.item);return{ok:true,status:'MILLION_BRANCH_IDEATION_ACTIVATION_READY',version:MILLION_BRANCH_IDEATION_GENOME_VERSION,matchedDomainIds:matched,selectedGenerators:selected,selectedGeneratorCount:selected.length,totalGeneratorCount:1000,candidateBudgetPerGenerator:budget,selectedFirstGenerationCapacity:selected.length*budget,wholeGenomeFirstGenerationCapacity:1000*budget,claimBoundary:'GENERATOR_OUTPUTS_ARE_HYPOTHESES_NOT_FACTS_CAPABILITIES_REVENUE_OR_EXTERNAL_PROOF',businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zero()};}
export function projectIdeationBranching({activeGenerators=1000,candidatesPerGenerator=50,survivorPercent=10,descendantsPerSurvivor=20,generations=2}={}){const a=bounded(activeGenerators,1,1000),c=bounded(candidatesPerGenerator,1,100),s=bounded(survivorPercent,1,100),d=bounded(descendantsPerSurvivor,1,100),n=bounded(generations,1,8);if(![a,c,s,d,n].every(Boolean))return{ok:false,status:'MILLION_BRANCH_PROJECTION_INVALID',reasonCodes:['bounded-branching-inputs-required'],businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zero()};let cur=a*c,total=cur;const counts=[cur];for(let i=2;i<=n;i++){cur=Math.max(1,Math.floor(cur*s/100))*d;if(!Number.isSafeInteger(cur)||!Number.isSafeInteger(total+cur))return{ok:false,status:'MILLION_BRANCH_PROJECTION_INVALID',reasonCodes:['projection-overflow'],businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zero()};counts.push(cur);total+=cur;}return{ok:true,status:'MILLION_BRANCH_PROJECTION_READY',generationCounts:counts,totalCandidateNodes:total,claimBoundary:'PROJECTED_COUNTS_ARE_NOT_EXECUTED_IDEAS_OR_DISCOVERIES',businessEffectAuthority:'NONE',externalEffectAuthority:'NONE',externalEffectLedger:zero()};}
