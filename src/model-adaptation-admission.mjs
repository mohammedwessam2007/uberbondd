import crypto from 'node:crypto';
import { ZERO_EXTERNAL_EFFECTS } from './effect-ledgers.mjs';

export const MODEL_ADAPTATION_ADMISSION_VERSION='uberbond.model-adaptation-admission.v1.1';
const ROUTES=Object.freeze(['PROMPT','RETRIEVAL','TOOLS','DECOMPOSITION','COMPOSITION','AUTHORIZED_EXISTING_MODEL']);
const PRIVACY=Object.freeze(['PUBLIC_NONPERSONAL','PERSONAL_WITH_CONSENT','SYNTHETIC']);
const SHA=/^[0-9a-f]{64}$/;
const text=(v,max=500)=>{const s=typeof v==='string'?v.trim():'';return s&&s.length<=max?s:null;};
const unique=v=>[...new Set(v.filter(Boolean))]; const hash=v=>crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex'); const zero=()=>structuredClone(ZERO_EXTERNAL_EFFECTS);
const fail=(status,reasons,extra={})=>({ok:false,version:MODEL_ADAPTATION_ADMISSION_VERSION,status,reasonCodes:unique(reasons),trainingAuthority:'NONE',deploymentAuthority:'NONE',businessEffectAuthority:'NONE',asiStatus:'SYSTEM_LEVEL_ASI_NOT_ESTABLISHED',externalEffectLedger:zero(),...extra});
const strictInt=v=>typeof v==='number'&&Number.isSafeInteger(v)&&v>=0?v:null; const strictNum=v=>typeof v==='number'&&Number.isFinite(v)&&v>=0?v:null;

function validateCheaperRoutes(rows,bottleneckDigest){const reasons=[];if(!Array.isArray(rows))return['cheaper-route-measurements-required'];if(rows.length!==ROUTES.length)reasons.push('all-cheaper-routes-must-be-measured-exactly-once');const seen=new Set();let prior=-1;for(const row of rows){const route=text(row?.route,80)?.toUpperCase();const index=ROUTES.indexOf(route);if(index<0){reasons.push('unknown-cheaper-route');continue;}if(seen.has(route))reasons.push('duplicate-cheaper-route');seen.add(route);if(index<=prior)reasons.push('cheaper-routes-must-follow-canonical-order');prior=index;if(row?.status!=='MEASURED_INSUFFICIENT')reasons.push(`cheaper-route-not-proven-insufficient:${route}`);if(!text(row?.evidenceRef,500))reasons.push(`cheaper-route-evidence-required:${route}`);if(text(row?.bottleneckDigest,64)?.toLowerCase()!==bottleneckDigest)reasons.push(`cheaper-route-bottleneck-mismatch:${route}`);if(!text(row?.matchedBudgetRef,500))reasons.push(`cheaper-route-matched-budget-required:${route}`);}for(const route of ROUTES)if(!seen.has(route))reasons.push(`cheaper-route-missing:${route}`);return reasons;}
function validateDataset(m){const reasons=[];const datasetDigest=text(m?.datasetDigest,64)?.toLowerCase();if(!datasetDigest||!SHA.test(datasetDigest))reasons.push('dataset-digest-required');const rows=Array.isArray(m?.sources)?m.sources:[];if(!rows.length)reasons.push('dataset-sources-required');for(const row of rows){if(!text(row?.provenanceRef,500))reasons.push('dataset-source-provenance-required');if(!text(row?.license,120)||row?.licenseStatus!=='ALLOWED'||!text(row?.licenseEvidenceRef,500))reasons.push('dataset-source-license-must-be-allowed-and-evidenced');const privacy=text(row?.privacyClass,80)?.toUpperCase();if(!PRIVACY.includes(privacy))reasons.push('dataset-source-privacy-class-required');if(privacy==='PERSONAL_WITH_CONSENT'&&(row?.explicitConsent!==true||!text(row?.consentRef,500)))reasons.push('personal-training-data-requires-explicit-consent-evidence');if(row?.collectionAuthorized!==true||!text(row?.collectionAuthorityRef,500))reasons.push('dataset-source-collection-authority-evidence-required');}
  const p=m?.partitions||{};const hashes=['trainHash','devHash','heldoutHash'].map(k=>text(p[k],64)?.toLowerCase());if(hashes.some(v=>!v||!SHA.test(v)))reasons.push('train-dev-heldout-hashes-required');else if(new Set(hashes).size!==3)reasons.push('train-dev-heldout-must-be-distinct');if(p?.identityOverlapCount!==0||!text(p?.overlapScanEvidenceRef,500))reasons.push('observed-zero-partition-overlap-evidence-required');
  const c=m?.contamination||{};if(c.evidenceClass!=='OBSERVED_SCAN'||c.knownBenchmarkMatchCount!==0||c.heldoutLeakCount!==0||!text(c.evidenceRef,500)||text(c.datasetDigest,64)?.toLowerCase()!==datasetDigest||text(c.heldoutHash,64)?.toLowerCase()!==hashes[2])reasons.push('observed-zero-contamination-evidence-must-bind-dataset-and-heldout');
  if(m?.syntheticDataPresent===true&&(m?.syntheticLabelsExplicit!==true||!text(m?.syntheticProvenanceRef,500)))reasons.push('synthetic-training-data-must-be-labeled-and-provenanced');if(!text(m?.adversarialDatasetRef,500))reasons.push('adversarial-evaluation-dataset-required');return reasons;}
function validateTraining(p,heldoutHash){const reasons=[];if(!text(p?.baseModelId,200)||!text(p?.baseModelRevision,300))reasons.push('base-model-identity-required');if(p?.baseModelLicenseStatus!=='ALLOWED'||!text(p?.baseModelLicenseEvidenceRef,500))reasons.push('base-model-license-must-be-allowed-and-evidenced');if(!['LORA','QLORA','FINE_TUNE','DISTILLATION'].includes(text(p?.method,80)?.toUpperCase()))reasons.push('recognized-adaptation-method-required');if(p?.isolation?.productionCredentialsMounted!==false||p?.isolation?.privateLifeStateMounted!==false||p?.isolation?.productionMutationReachable!==false)reasons.push('isolated-training-boundary-required');if(p?.isolation?.networkEgressMode!=='DATASET_AND_MODEL_REGISTRY_ALLOWLIST'||!text(p?.isolation?.networkAllowlistRef,500))reasons.push('training-network-must-be-evidenced-allowlist');if(strictInt(p?.budget?.maxComputeUnits)===null||strictInt(p?.budget?.maxSpendCents)===null)reasons.push('explicit-training-resource-limits-required');if(!text(p?.reproducibility?.seedRef,500)||!text(p?.reproducibility?.environmentDigest,64)||!SHA.test(p.reproducibility.environmentDigest))reasons.push('training-reproducibility-contract-required');if(text(p?.heldoutHash,64)?.toLowerCase()!==heldoutHash)reasons.push('training-plan-heldout-binding-mismatch');return reasons;}
function validateEvaluation(p,heldoutHash){const reasons=[];if(!text(p?.c13EvaluationContractRef,500)||!text(p?.strongestBaselineRef,500))reasons.push('c13-and-strongest-baseline-evaluation-required');if(text(p?.heldoutHash,64)?.toLowerCase()!==heldoutHash)reasons.push('evaluation-heldout-binding-mismatch');if(p?.evaluatorIndependent!==true)reasons.push('independent-evaluator-required');if(p?.frozenBeforeTraining!==true)reasons.push('evaluation-protocol-must-be-frozen-before-training');if(p?.trainingDataAccessibleToEvaluator===true)reasons.push('evaluator-must-not-receive-training-data');if(p?.candidateArtifactDigestRequiredBeforeEvaluation!==true)reasons.push('post-training-artifact-digest-required-before-evaluation');return reasons;}
function validateServing(p){const reasons=[];if(p?.candidateArtifactDigest!=null)reasons.push('pretraining-cannot-claim-future-candidate-artifact-digest');if(p?.candidateArtifactDigestRequiredBeforeCanary!==true)reasons.push('candidate-artifact-digest-required-before-canary');if(!text(p?.rollbackArtifactRef,500)||!text(p?.priorServingRevision,300))reasons.push('serving-rollback-contract-required');if(!text(p?.canaryPlanRef,500)||!text(p?.c26SecurityAdmissionRef,500))reasons.push('canary-and-c26-security-gates-required');if(strictNum(p?.maxAllowedRegression)===null)reasons.push('serving-regression-threshold-required');const qs=Array.isArray(p?.quantizationPlan)?p.quantizationPlan:[];if(!qs.length)reasons.push('quantization-plan-required');const formats=new Set();for(const q of qs){const format=text(q?.format,80);if(!format||formats.has(format)||q?.artifactDigest!=null||q?.mustRebenchmark!==true)reasons.push('quantization-plan-requires-unique-format-future-digest-and-rebenchmark');if(format)formats.add(format);}if(p?.automaticProductionPromotion===true)reasons.push('automatic-production-promotion-prohibited');return reasons;}

/** Pure pre-training C16 admission gate. It authorizes neither training nor serving. */
export function admitModelAdaptation({bottleneck={},cheaperRoutes=[],dataset={},trainingPlan={},evaluationPlan={},servingPlan={}}={}){const bottleneckDigest=text(bottleneck?.bottleneckDigest,64)?.toLowerCase();if(!bottleneckDigest||!SHA.test(bottleneckDigest)||!text(bottleneck?.evidenceRef,500))return fail('MODEL_ADAPTATION_PROTOCOL_INVALID',['evidence-bound-bottleneck-required']);const reasons=[...validateCheaperRoutes(cheaperRoutes,bottleneckDigest),...validateDataset(dataset)];const heldout=text(dataset?.partitions?.heldoutHash,64)?.toLowerCase();reasons.push(...validateTraining(trainingPlan,heldout),...validateEvaluation(evaluationPlan,heldout),...validateServing(servingPlan));if(reasons.length){const cheaper=reasons.some(r=>r.startsWith('cheaper-route'));return fail(cheaper?'MODEL_ADAPTATION_DEFER_TO_CHEAPER_ROUTE':'MODEL_ADAPTATION_ADMISSION_REFUSED',reasons,{bottleneckDigest});}const admission={bottleneckDigest,cheaperRouteEvidence:cheaperRoutes.map(r=>({route:r.route,evidenceRef:r.evidenceRef,matchedBudgetRef:r.matchedBudgetRef})),datasetDigest:dataset.datasetDigest.toLowerCase(),heldoutHash:heldout,baseModel:{id:trainingPlan.baseModelId,revision:trainingPlan.baseModelRevision},method:trainingPlan.method.toUpperCase(),futureArtifactBindingRequired:true,rollbackArtifactRef:servingPlan.rollbackArtifactRef,maxAllowedRegression:servingPlan.maxAllowedRegression,quantizationFormats:servingPlan.quantizationPlan.map(q=>q.format)};return{ok:true,version:MODEL_ADAPTATION_ADMISSION_VERSION,status:'MODEL_ADAPTATION_ADMISSIBLE_FOR_SEPARATE_ISOLATED_TRAINING_AUTHORITY',admission,admissionDigest:hash(admission),truthBoundary:'PRETRAINING_ADMISSION_PROVES_PRECONDITIONS_ONLY. NO_FUTURE_MODEL_OR_QUANTIZED_ARTIFACT_DIGEST_IS_CLAIMED. IT_DOES_NOT_TRAIN_PROMOTE_SERVE_OR_ESTABLISH_IMPROVEMENT_OR_ASI.',trainingAuthority:'NONE',deploymentAuthority:'NONE',businessEffectAuthority:'NONE',asiStatus:'SYSTEM_LEVEL_ASI_NOT_ESTABLISHED',externalEffectLedger:zero()};}
