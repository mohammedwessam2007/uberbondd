import test from 'node:test';import assert from 'node:assert/strict';import { compileArtificialImagination as c } from '../src/artificial-imagination.mjs';test('Artificial Imagination generates bounded constrained hypothesis branches rather than reality claims',()=>{const r=c({seedEvidenceRefs:['e1'],constraints:['c1','c2'],branchBudget:16});assert.equal(r.ok,true);assert.equal(r.payload.branches.length,16);assert.equal(r.payload.notRealityClaims,true);});test('Artificial Imagination refuses unbounded or evidence-free imagination',()=>{assert.equal(c({seedEvidenceRefs:[],constraints:['c'],branchBudget:999999}).ok,false);});
