import test from 'node:test'; import { compileBuyerClassifyIntent } from '../src/buyer-classify-intent.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'buyer.classify-intent',compile:compileBuyerClassifyIntent}; test('buyer.classify-intent compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('buyer.classify-intent refuses provider authority escalation',()=>h(x)); test('buyer.classify-intent recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
