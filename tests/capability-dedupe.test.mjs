import test from 'node:test'; import { compileCapabilityDedupe } from '../src/capability-dedupe.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'capability.dedupe',compile:compileCapabilityDedupe}; test('capability.dedupe compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('capability.dedupe refuses provider authority escalation',()=>h(x)); test('capability.dedupe recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
