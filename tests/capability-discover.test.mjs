import test from 'node:test'; import { compileCapabilityDiscover } from '../src/capability-discover.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'capability.discover',compile:compileCapabilityDiscover}; test('capability.discover compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('capability.discover refuses provider authority escalation',()=>h(x)); test('capability.discover recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
