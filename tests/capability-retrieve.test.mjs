import test from 'node:test'; import { compileCapabilityRetrieve } from '../src/capability-retrieve.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'capability.retrieve',compile:compileCapabilityRetrieve}; test('capability.retrieve compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('capability.retrieve refuses provider authority escalation',()=>h(x)); test('capability.retrieve recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
