import test from 'node:test'; import { compileCapabilityRevoke } from '../src/capability-revoke.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'capability.revoke',compile:compileCapabilityRevoke}; test('capability.revoke compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('capability.revoke refuses provider authority escalation',()=>h(x)); test('capability.revoke recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
