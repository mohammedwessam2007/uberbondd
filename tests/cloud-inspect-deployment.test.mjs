import test from 'node:test'; import { compileCloudInspectDeployment } from '../src/cloud-inspect-deployment.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'cloud.inspect-deployment',compile:compileCloudInspectDeployment}; test('cloud.inspect-deployment compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('cloud.inspect-deployment refuses provider authority escalation',()=>h(x)); test('cloud.inspect-deployment recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
