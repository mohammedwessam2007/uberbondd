import test from 'node:test'; import { compileCodeSearch } from '../src/code-search.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'code.search',compile:compileCodeSearch}; test('code.search compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('code.search refuses provider authority escalation',()=>h(x)); test('code.search recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
