import test from 'node:test'; import { compileContextCompressReversible } from '../src/context-compress-reversible.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'context.compress-reversible',compile:compileContextCompressReversible}; test('context.compress-reversible compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('context.compress-reversible refuses provider authority escalation',()=>h(x)); test('context.compress-reversible recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
