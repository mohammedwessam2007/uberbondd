import test from 'node:test'; import { compileDataExtractStructured } from '../src/data-extract-structured.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'data.extract-structured',compile:compileDataExtractStructured}; test('data.extract-structured compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('data.extract-structured refuses provider authority escalation',()=>h(x)); test('data.extract-structured recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
