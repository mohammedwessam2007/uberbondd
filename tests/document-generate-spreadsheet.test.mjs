import test from 'node:test'; import { compileDocumentGenerateSpreadsheet } from '../src/document-generate-spreadsheet.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'document.generate-spreadsheet',compile:compileDocumentGenerateSpreadsheet}; test('document.generate-spreadsheet compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('document.generate-spreadsheet refuses provider authority escalation',()=>h(x)); test('document.generate-spreadsheet recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
