import test from 'node:test';import assert from 'node:assert/strict';import { compileFutureCapitalReserve as c } from '../src/future-capital-reserve.mjs';test('Future Capital Reserve preserves bounded option resources without spend authority',()=>{const r=c({totalUnits:100,reserveUnits:20,reservePurposeRef:'future-options'});assert.equal(r.ok,true);assert.equal(r.payload.spendAuthority,'NONE');});test('Future Capital Reserve refuses negative or over-total reserves',()=>{assert.equal(c({totalUnits:100,reserveUnits:120,reservePurposeRef:'x'}).ok,false);});
