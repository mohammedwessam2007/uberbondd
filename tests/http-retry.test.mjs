import test from 'node:test'; import { compileHttpRetry } from '../src/http-retry.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'http.retry',compile:compileHttpRetry}; test('http.retry compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('http.retry refuses provider authority escalation',()=>h(x)); test('http.retry recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
