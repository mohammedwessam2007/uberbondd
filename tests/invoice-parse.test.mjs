import test from 'node:test'; import { compileInvoiceParse } from '../src/invoice-parse.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'invoice.parse',compile:compileInvoiceParse}; test('invoice.parse compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('invoice.parse refuses provider authority escalation',()=>h(x)); test('invoice.parse recovery rebinds from refused hostile evidence to a clean local plan',()=>r(x));
