import test from 'node:test'; import { compileModelRouteRequest } from '../src/model-route-request.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'model.route-request',compile:compileModelRouteRequest}; test('model.route-request compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('model.route-request refuses provider authority escalation',()=>h(x)); test('model.route-request recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
