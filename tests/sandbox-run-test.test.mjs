import test from 'node:test'; import { compileSandboxRunTest } from '../src/sandbox-run-test.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'sandbox.run-test',compile:compileSandboxRunTest}; test('sandbox.run-test compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('sandbox.run-test refuses provider authority escalation',()=>h(x)); test('sandbox.run-test recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
