import test from 'node:test';
import assert from 'node:assert/strict';
import crypto from 'node:crypto';
import { evaluateSystemLevelAsiEvidence, CANONICAL_ASI_DIMENSIONS, REQUIRED_C13_EVALUATION_VERSION } from '../src/system-level-asi-evidence.mjs';
const hash=v=>crypto.createHash('sha256').update(JSON.stringify(v)).digest('hex');
const D=i=>(String(i).repeat(64)).slice(0,64).replace(/[^0-9a-f]/g,'a');
function c13(i){const receipt={version:REQUIRED_C13_EVALUATION_VERSION,compositionDigest:'a'.repeat(64),compositionId:'uberbond.candidate',compositionRevision:'candidate@1',familyMeasurements:[{familyId:`dimension-${i}`,taskSetHash:i.toString(16).padStart(64,'0'),measurementEvidenceRef:`measurement://${i}`}],transferReceiptHash:D((i%8)+1),retentionReceiptHash:D(((i+1)%8)+1)};return{ok:true,version:REQUIRED_C13_EVALUATION_VERSION,status:'COMPOUND_INTELLIGENCE_GAIN_SUPPORTED_WITHIN_DEFINED_SCOPE',evidenceStage:'FRESH_CONTEXT_REPRODUCED_WITH_CROSS_DOMAIN_TRANSFER_SUPPORT',asiStatus:'SYSTEM_LEVEL_ASI_NOT_ESTABLISHED',compositionDigest:receipt.compositionDigest,compositionId:receipt.compositionId,compositionRevision:receipt.compositionRevision,generalityClaim:'WITHHELD__DEFINED_TASK_POPULATION_ONLY',receipt,receiptHash:hash(receipt)};}
const baseRow=(dimension,i)=>({dimension,evidenceRef:`evidence://c21/${i}`,independentEvaluatorRef:`evaluator://independent/${i}`,verifierId:`verifier-${i%4}`,verifierLineageRef:`lineage://verifier/${i%4}`,taskPopulationHash:i.toString(16).padStart(64,'0'),observedAt:'2026-09-09T02:00:00Z',baselineVerifiedAtEvaluationTime:true,thresholdFrozenBeforeEvaluation:true,freshContextHeldout:true,freshContextRetained:true,contaminationStatus:'CLEAN',evaluatorIndependent:true,compoundEvaluation:c13(i)});
const rows=()=>CANONICAL_ASI_DIMENSIONS.map((d,i)=>baseRow(d,i));
const run=(dimensions=rows())=>evaluateSystemLevelAsiEvidence({candidateId:'uberbond.candidate',candidateRevision:'candidate@1',dimensions,observedAt:'2026-09-09T02:30:00Z'});
test('all canonical dimensions can earn strong-within-defined-scope evidence while ASI remains not established',()=>{const r=run();assert.equal(r.ok,true);assert.equal(r.evidenceStage,'SYSTEM_LEVEL_ASI_EVIDENCE_STRONG_WITHIN_DEFINED_SCOPE');assert.equal(r.asiStatus,'SYSTEM_LEVEL_ASI_NOT_ESTABLISHED');assert.equal(r.selfModificationAuthority,'NONE');});
test('missing a critical dimension prevents partial stage even with many other dimensions',()=>{const r=run(rows().filter(x=>x.dimension!=='adversarial robustness'));assert.equal(r.ok,true);assert.equal(r.evidenceStage,'SYSTEM_LEVEL_ASI_EVIDENCE_NOT_ESTABLISHED');});
test('subset covering critical dimensions and enough breadth can only earn partial evidence',()=>{const keep=new Set(['causal reasoning','planning under uncertainty','long-horizon coherence','transfer between domains','self-improvement','adversarial robustness','calibrated refusal and ignorance detection','novel problem solving','scientific discovery','software engineering','mathematical reasoning','forecasting']);const r=run(rows().filter(x=>keep.has(x.dimension)));assert.equal(r.evidenceStage,'SYSTEM_LEVEL_ASI_EVIDENCE_PARTIAL');assert.equal(r.asiStatus,'SYSTEM_LEVEL_ASI_NOT_ESTABLISHED');});
test('duplicate evidence cannot be cloned across dimensions',()=>{const x=rows();x[1].evidenceRef=x[0].evidenceRef;const r=run(x);assert.equal(r.ok,false);assert.ok(r.reasonCodes.includes('one-observed-evidence-record-cannot-be-cloned-across-dimensions'));});
test('task population reuse cannot inflate cross-domain breadth',()=>{const x=rows();x[1].taskPopulationHash=x[0].taskPopulationHash;const r=run(x);assert.equal(r.ok,false);assert.ok(r.reasonCodes.includes('task-population-hash-reuse-across-dimensions-refused'));});
test('stale evidence is refused',()=>{const x=rows();x[0].observedAt='2025-01-01T00:00:00Z';assert.equal(run(x).ok,false);});
test('wrong candidate C13 evidence is refused',()=>{const x=rows();x[0].compoundEvaluation={...x[0].compoundEvaluation,compositionId:'other'};assert.equal(run(x).ok,false);});
test('fresh-context retention must be explicit',()=>{const x=rows();x[0].freshContextRetained=false;assert.equal(run(x).ok,false);});
test('post-hoc threshold freezing is refused',()=>{const x=rows();x[0].thresholdFrozenBeforeEvaluation=false;assert.equal(run(x).ok,false);});
test('baseline must be verified at evaluation time',()=>{const x=rows();x[0].baselineVerifiedAtEvaluationTime=false;assert.equal(run(x).ok,false);});
test('correlated verifier identities cannot satisfy independence minimum',()=>{const x=rows().map(r=>({...r,verifierId:'same',verifierLineageRef:'lineage://same'}));const r=run(x);assert.equal(r.ok,true);assert.equal(r.evidenceStage,'SYSTEM_LEVEL_ASI_EVIDENCE_NOT_ESTABLISHED');});
test('cosmetic role prefix casing and whitespace aliases cannot inflate verifier diversity',()=>{const aliases=['Verifier: Eval-A',' eval-a ','EVALUATOR: EVAL-A','reviewer: eval-a'];const lineages=[' LINEAGE://SAME ','lineage://same','lineage://SAME','lineage://same'];const x=rows().map((r,i)=>({...r,verifierId:aliases[i%aliases.length],verifierLineageRef:lineages[i%lineages.length]}));const result=run(x);assert.equal(result.ok,true);assert.equal(result.counts.independentVerifierIdentities,1);assert.equal(result.counts.independentVerifierLineages,1);assert.equal(result.evidenceStage,'SYSTEM_LEVEL_ASI_EVIDENCE_NOT_ESTABLISHED');});
test('one evaluator record cannot be cosmetically relabeled across dimensions',()=>{const x=rows();x[1].independentEvaluatorRef=' EVALUATOR://INDEPENDENT/0 ';const r=run(x);assert.equal(r.ok,false);assert.ok(r.reasonCodes.includes('one-independent-evaluator-record-cannot-be-cloned-across-dimensions'));});
test('unexpected dimensions are refused instead of expanding denominator ad hoc',()=>{const x=rows();x.push(baseRow('telepathy',99));const r=run(x);assert.equal(r.ok,false);assert.ok(r.reasonCodes.includes('only-canonical-c21-dimensions-may-count'));});
test('forged C13 payload with stale receipt hash is refused',()=>{const x=rows();x[0].compoundEvaluation={...x[0].compoundEvaluation,receipt:{...x[0].compoundEvaluation.receipt,compositionRevision:'forged'}};assert.equal(run(x).ok,false);});
test('one authentic C13 receipt cannot be relabeled across two dimensions',()=>{const x=rows();x[1].compoundEvaluation=structuredClone(x[0].compoundEvaluation);x[1].taskPopulationHash='f'.repeat(64);const r=run(x);assert.equal(r.ok,false);assert.ok(r.reasonCodes.includes('one-c13-evaluation-receipt-cannot-be-cloned-across-dimensions'));});
