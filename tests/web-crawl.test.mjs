import test from 'node:test'; import { compileWebCrawl } from '../src/web-crawl.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'web.crawl',compile:compileWebCrawl}; test('web.crawl compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('web.crawl refuses provider authority escalation',()=>h(x)); test('web.crawl recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
