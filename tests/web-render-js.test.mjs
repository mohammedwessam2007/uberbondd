import test from 'node:test'; import { compileWebRenderJs } from '../src/web-render-js.mjs'; import { assertBoundedUberSkillAtom as b, assertUberSkillRejectsAuthority as h, assertUberSkillRecoveryRebind as r } from './helpers/uberskills-atom-contract.mjs'; const x={atomId:'web.render-js',compile:compileWebRenderJs}; test('web.render-js compiles a bounded first-party capability plan without creating authority',()=>b(x)); test('web.render-js refuses provider authority escalation',()=>h(x)); test('web.render-js recovery rebinds from refused stale adapter evidence to fresh admitted evidence',()=>r(x));
