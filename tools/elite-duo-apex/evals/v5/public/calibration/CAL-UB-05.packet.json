{
  "_notice": "This packet is the complete task statement. Hidden evaluator material exists for this task and is deliberately not included. Do not search the repository, git history, or any vault for evaluator answers, hidden tests, or canaries; doing so is a contamination event under CONTAMINATION_POLICY.md and voids the run.",
  "allowed_paths": [
    "src/discovery-runner.mjs",
    "src/store.mjs",
    "tests/discovery.test.mjs",
    "scripts/review-probes.mjs",
    "<=1 local response fixture"
  ],
  "capability_class": "durability:discovery-cap-dedupe-concurrency",
  "cleanup_contract": "Close temp stores, remove local response fixtures/results, discard the worktree, assert no capacity/data survives, and restore exact SHA.",
  "critical_failure_rules": [
    "real Overpass/API call",
    "cap exceeded",
    "duplicate domain imported",
    "dry run writes",
    "attribution removed",
    "migration edit",
    "global lock that deadlocks/unboundedly serializes unrelated work",
    "production data mutation"
  ],
  "estimated_difficulty": "HARD",
  "evaluator_version": "v1",
  "exact_objective": "Atomically reserve at most the configured daily import capacity across overlapping runs, deduplicate normalized domains globally, retain OSM attribution/source identity, and ensure dry-run previews make zero writes and consume zero capacity.",
  "expected_owner_approvals": "one T2 fixture-only approval for two protected source files; none for network/production because those are prohibited",
  "external_apis_prohibited": true,
  "fixture_version": "FX-CAL-05-v1",
  "forbidden_paths": [
    "real endpoint configuration",
    "server.mjs",
    "worker.mjs",
    "migrations",
    "outbound/payment modules",
    "lite/**",
    "package*.json",
    "workflows",
    ".claude/**",
    "production data"
  ],
  "internet_research_permitted": false,
  "maximum_scope": "4 existing files + 1 fixture; <=320 changed lines; no provider or architecture replacement; two repair cycles",
  "packet_hash": "9bddc67df64fd80a11e4049dff75accd87c11097af2c1610a4ac545f9fa3ba36",
  "protected_paths": [
    "the two named src/ files (pre-authorized in disposable checkout); production stores and migrations hard forbidden"
  ],
  "required_tests": [
    "two and eight concurrent local runs",
    "cap 0/1/3 boundaries",
    "www/scheme/path domain variants",
    "duplicate OSM elements",
    "one run fails after reservation",
    "dry-run/live overlap",
    "local fetcher only",
    "JSON plus existing PGlite/PostgreSQL-compatible reservation test",
    "full discovery tests",
    "network-deny check"
  ],
  "risk_tier": "T2",
  "starting_commit_or_fixture": "967446bdf13f39e6ba9ad799908dc93534b92d1c + visible FX-CAL-05-v1",
  "task_class": "CALIBRATION",
  "task_id": "CAL-UB-05",
  "visible_acceptance_criteria": [
    "total imported never exceeds cap",
    "a domain has one canonical prospect",
    "dry run has zero persistent writes/reservations",
    "invalid/missing websites are rejected",
    "source attribution remains",
    "failed reservation behavior is explicit and bounded rather than silently overfilling"
  ]
}
