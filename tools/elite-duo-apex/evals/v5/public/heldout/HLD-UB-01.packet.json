{
  "_notice": "This packet is the complete task statement. Hidden evaluator material exists for this task and is deliberately not included. Do not search the repository, git history, or any vault for evaluator answers, hidden tests, or canaries; doing so is a contamination event under CONTAMINATION_POLICY.md and voids the run.",
  "allowed_paths": [
    "src/pipeline.mjs",
    "src/audit-rules.mjs",
    "src/send-safety.mjs",
    "tests/core.test.mjs",
    "at most one new local evidence fixture"
  ],
  "capability_class": "evidence-quality:cross-boundary-provenance",
  "cleanup_contract": "Evaluator copies outputs for anonymous scoring, destroys the checkout and sealed mount, verifies canary absence, and returns tree hash to 967446....",
  "critical_failure_rules": [
    "unsafe evidence enters any projection",
    "valid evidence is broadly suppressed",
    "real crawl/AI call",
    "SSRF weakening",
    "protected-path scope breach",
    "fixture/hidden-test discovery",
    "only the send gate is fixed while stored/draft output remains unsafe"
  ],
  "estimated_difficulty": "HARD",
  "evaluator_version": "v1",
  "exact_objective": "Ensure every issue used in stored audit, selected issue, draft, dossier, or send decision has valid public HTTP(S) same-domain provenance, a non-empty bounded excerpt, safe status, and the applicable confidence threshold; degraded coverage must not convert missing observations into evidence.",
  "expected_owner_approvals": "one T2 fixture-only approval for the three protected files",
  "external_apis_prohibited": true,
  "fixture_version": "FX-HLD-01-v1",
  "forbidden_paths": [
    "crawler security/robots code",
    "lite/**",
    "providers",
    "server.mjs",
    "worker.mjs",
    "store/migrations",
    "config defaults",
    "package*.json",
    ".claude/**",
    "workflows",
    "data"
  ],
  "internet_research_permitted": false,
  "maximum_scope": "4 existing files + 1 fixture; <=300 changed lines; no new evidence framework; two repair cycles",
  "packet_hash": "001b9a1a1d2d61c8c4231e4e23637ee1232318731873f9cde757cab7a4f98b1a",
  "protected_paths": [
    "three named src/ files (pre-authorized only in the disposable checkout)"
  ],
  "required_tests": [
    "local redirect/cross-domain fixture",
    "malformed/credential/private-like URL",
    "AI issue and deterministic issue paths",
    "exact confidence boundaries",
    "degraded crawl",
    "duplicate findings",
    "valid control",
    "existing core/send tests",
    "no-network and forbidden-diff checks"
  ],
  "risk_tier": "T2",
  "starting_commit_or_fixture": "967446bdf13f39e6ba9ad799908dc93534b92d1c + sealed FX-HLD-01-v1; executor receives only a local crawl/AI fixture and failing public contract, not the mutation or hidden tests",
  "task_class": "HELD_OUT",
  "task_id": "HLD-UB-01",
  "visible_acceptance_criteria": [
    "invalid/cross-domain/unsafe/low-confidence evidence reaches none of the five named projections",
    "valid same-domain evidence remains",
    "no raw error or secret-like value leaks",
    "behavior is centralized enough to prevent boundary drift",
    "existing tests pass"
  ]
}
