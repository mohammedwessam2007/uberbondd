Run `elite-independent-reviewer` in a fresh context.

When the verdict is `REPAIR_REQUIRED`, create `REPAIR_CONTRACT.json` and run `elite-debugger` or `elite-executor` according to severity.

Repeat the review once after repair. Stop after an allowed final verdict.
